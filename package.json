{
  "name": "deepseek-harness-desktop",
  "version": "0.1.0",
  "description": "DeepSeek Harness Desktop V0.1 — native macOS desktop shell for DeepSeek Harness 0.1.0-rc.6, with a safe DIY appearance system",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:frontend": "vite build",
    "dev:frontend": "vite",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test:engine": "node tests/engine.test.mjs",
    "test:guard": "node tests/session_guard.test.mjs",
    "test": "npm run typecheck && npm run test:engine && npm run test:guard"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5",
    "vite": "^6"
  }
}
